routes
('/', projectView.initIndex)
('/projects/:id', func that shows only the clicked project projectView.handleClicks-ish)
// or maybe use the title instead since #s wont be in the non-placeholder titles
('/projects/:title', func that shows only the clicked project projectView.handleClicks-ish)
